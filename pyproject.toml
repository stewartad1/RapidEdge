[build-system]
requires = ["setuptools>=67.0"]
build-backend = "setuptools.build_meta"

[project]
name = "rapidedge"
version = "0.1.0"
description = "FastAPI service for parsing DXF uploads into structured JSON"
readme = "README.md"
requires-python = ">=3.11"

[tool.setuptools]
packages = ["app"]

